#include <bits/stdc++.h>
using namespace std;

int main()
{
    int times;
    cin >> times;
    while (times--)
    {
        int n;
        cin >> n;
        int newStarting = n;
        for (int i = newStarting;; i++)
        {
            if (n % 3 != 0 || n % 10 == 3)
            {
            }
        }
    }

    return 0;
}